node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.tsbuildinfo
.claude/

# Env files with secrets
.env
.env.local

# Question extraction scripts
scripts/.env

# Firebase service account key — never commit
scripts/service-account.json
service-account.json
scripts/progress/
scripts/pdfs/

scripts/.import-workspace/
scripts/output/

# QC audit working files (full question-bank dumps — too big, and not source)
scripts/qc/data/

# Python bytecode cache
__pycache__/
*.pyc

# any Firebase admin key variant (prod, test, etc.) — this also catches the
# default filename Firebase Console downloads use (<project>-firebase-adminsdk-<hash>.json)
scripts/service-account*.json
service-account*.json
*firebase-adminsdk*.json

# reference-only example project, not part of this app
scripts/reference_concept_crack/
